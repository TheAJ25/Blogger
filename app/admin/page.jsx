import React from 'react';
import AddProductPage from '@/app/admin/addProduct/page'; // Rename to a proper component name

const page = () => {
  return (
    <AddProductPage />
  );
};

export default page;
